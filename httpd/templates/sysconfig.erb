<% if @options and @options.kind_of?(Array) and ! @options.empty? -%>
OPTIONS="-D<%= @options.reject(&:empty?).join(' -D') -%> -D<%= @hostname.upcase %>"
<% else -%>
OPTIONS="-D<%= @hostname.upcase %>"
<% end -%>

<% if @lang and ! @lang.empty? -%>
HTTPD_LANG=<%= @lang %>
<% end -%>
