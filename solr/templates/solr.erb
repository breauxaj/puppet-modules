[Unit]
Description=Apache Solr
After=network.target
 
[Service]
Type=forking
EnvironmentFile=/etc/sysconfig/solr
ExecStart=<%= @solr_root %>/bin/solr start $CLOUD $PORT
ExecStop=<%= @solr_root %>/bin/solr stop
User=solr

[Install]
WantedBy=multi-user.target